/* ============================================================
   PENEIRAS ON — configuração do Tailwind CSS v4
   Carregado por `@config` em assets/css/input.css.

   Este arquivo só declara ONDE o Tailwind procura classes (content).
   Os tokens do design system (cores, fontes, espaçamentos, raios,
   sombras) NÃO ficam aqui: vivem no bloco `@theme` de
   assets/css/input.css, que é a forma nativa da v4.
   ============================================================ */
module.exports = {
  content: [
    './*.html',
    './assets/js/*.js',
  ],
};
